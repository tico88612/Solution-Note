#include <bits/stdc++.h>
#define _ ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;
int main(){
	long long int N;
	cin>>N;
	if(N==1)
		cout<<"0"<<'\n';
	else
		cout<<"1"<<'\n';
	
	return 0;
}